[build]
builder = "railpack"
watchPatterns = [
  "railway.toml",
  "Procfile",
  "requirements.txt",
  "nemoclaw/scripts/**",
  "backend/scripts/**",
  "autoresearch/data/**",
  "frontend/**",
  "docs/**"
]

[deploy]
startCommand = "python3 nemoclaw/scripts/nemotron_coordinator.py"
restartPolicyType = "on_failure"
